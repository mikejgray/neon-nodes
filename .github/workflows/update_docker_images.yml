name: Publish Updated Docker image
on:
  workflow_dispatch:

jobs:
  build_and_publish_docker_websocket:
    uses: neongeckocom/.github/.github/workflows/publish_docker.yml@master
    secrets: inherit
    with:
      image_name: neon-node-websocket
      dockerfile: dockerfiles/websocket.dockerfile
  build_and_publish_docker_voice:
    uses: neongeckocom/.github/.github/workflows/publish_docker.yml@master
    secrets: inherit
    with:
      image_name: neon-node-voice
      dockerfile: dockerfiles/voice.dockerfile